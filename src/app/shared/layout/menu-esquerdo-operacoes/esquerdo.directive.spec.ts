import { EsquerdoDirective } from './esquerdo.directive';

describe('EsquerdoDirective', () => {
  it('should create an instance', () => {
    const directive = new EsquerdoDirective();
    expect(directive).toBeTruthy();
  });
});
